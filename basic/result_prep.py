import os
from dotenv import load_dotenv
import classes

def to_json(input):
    '''Function to transform and save a string into a json file'''
    pass


def to_pdf(input):
    '''Function to transform an markdown text into a saved pdf file'''
